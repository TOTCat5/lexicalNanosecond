#include "parse.h"

/*
    interLang is formed as "op(result,arg0,arg1)"

    the types are basically the normal types but in uppercase 
*/





void generateInterLangCode(AST_Node *tree,FILE *outputFile);


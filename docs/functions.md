**Functions** are declared as "funcName:type(args:type,(...))" and can be defined in two ways:<br>
**1st** way:<br>
    first the function declaration, then an expression.<br>
**2nd** way:<br>
    i... i can't really define it... it's basically like that:"rectangleArea:double(width:double\[width>=0\],height:double\[height>=0\])=width*height;"
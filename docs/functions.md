**Functions** are declared as "funcName(args:type,(...)):type" and can be defined in two ways:<br>
**1st** way:<br>
    first the function declaration, then an expression.<br>
**2nd** way:<br>
    i... i can't really define it... it's basically like that:"rectangleArea(width:double\[width>=0\],height:double\[height>=0\]):double=width*height;"
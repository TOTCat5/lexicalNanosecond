**Modifiers** are modifications of a type,like turning it into a pointer (with the 'ptr' modifier) or a linked list (with the 'linkedList' modifier).<br>
Modifiers are written as such:<br>
"variableName:modifier(type)=value" 
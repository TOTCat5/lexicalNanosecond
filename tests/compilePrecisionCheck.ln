
\\it's not the last version of this check,i just don't support modulos and equalizations in parsing right now
doSomeMath:uint64(x:int64[i-5],y:int64)=x*y;

main():int32
{
    \\ should give an error
    area:uint64=doSomeMath(3,3);


    return -1;
}

